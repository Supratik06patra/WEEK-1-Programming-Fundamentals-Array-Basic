** Determine Whether The Array Elements Are In Ascending Order.
Input: 1 2 3 5 

def is_ascending(arr):

  for i in range(len(arr) - 1):
    if arr[i] > arr[i + 1]:
      return False
  return True

array = [1, 2, 3, 5]
print("Ascending:", is_ascending(array))
